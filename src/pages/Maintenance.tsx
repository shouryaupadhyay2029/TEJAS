import React from 'react';

export const Maintenance: React.FC = () => {
  return (
    <div style={{ padding: '4rem 2rem', color: '#faf6f0' }}>
      <h1 style={{ fontFamily: 'var(--font-display)', fontSize: '2.5rem', marginBottom: '1rem' }}>Maintenance Planning</h1>
      <p style={{ color: 'var(--color-text-muted)' }}>Blank slate maintenance planning. Ready to rebuild...</p>
    </div>
  );
};

export default Maintenance;
