import React from 'react';

export const BlockPlanning: React.FC = () => {
  return (
    <div style={{ padding: '4rem 2rem', color: '#faf6f0' }}>
      <h1 style={{ fontFamily: 'var(--font-display)', fontSize: '2.5rem', marginBottom: '1rem' }}>Block Planning</h1>
      <p style={{ color: 'var(--color-text-muted)' }}>Blank slate block planning. Ready to rebuild...</p>
    </div>
  );
};

export default BlockPlanning;
