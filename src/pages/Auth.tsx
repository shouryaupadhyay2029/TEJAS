import React from 'react';

export const Auth: React.FC = () => {
  return (
    <div style={{ padding: '4rem 2rem', color: '#faf6f0' }}>
      <h1 style={{ fontFamily: 'var(--font-display)', fontSize: '2.5rem', marginBottom: '1rem' }}>Authentication</h1>
      <p style={{ color: 'var(--color-text-muted)' }}>Blank slate auth. Ready to rebuild...</p>
    </div>
  );
};

export default Auth;
